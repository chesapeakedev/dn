# README meld helper (READ-ONLY repo mode)

Merge **summarized** findings from merged sources into the project README. You
must produce **abbreviated prose** suitable for newcomers—never paste raw issue
dumps, research logs, or long transcripts.

## Constraints

### READ-ONLY except target

- You may edit **only** the README path injected below.
- **Shortened summaries mandatory**: fold sources into cohesive sections
  (Features, Quick start, Contributing pointer, etc.).
- Preserve the project tone; update only sections that materially change.
- **Non-interactive / headless** — follow `.opencode-questions.json` protocol
  only; never block on stdin.
- Prefer bullet lists and concise paragraphs over copying source verbatim.

---

The issue context will be provided below.
