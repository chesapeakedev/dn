{
  "schema_version": "2.0",
  "agent": "opencode",
  "harness_hints": {
    "vcs": "Sapling (`sl`). Never run git commands in this repo.",
    "close_out": "After kickstart/loop, ask before committing. If yes, this chat writes the commit with the repo VCS and omits `*.plan.md`. Do not auto-run `dn land`. Use `dn land` in CLI, CI, denoise, `--issue-testplan`, or when the user names it.",
    "trunk": "`make sync` / `dn sync` publishes to trunk after a local commit. Distinct from land.",
    "nested_agent": "Do not pass `--agent cursor` unless asked."
  },
  "sync": {
    "preflight": [["make", "lint"], ["make", "tests"]],
    "trunk": "main"
  },
  "ensure": {
    "lint": {
      "argv": ["make", "lint"],
      "intent": "Fix format, typecheck, and lint failures until make lint exits 0."
    },
    "tests": {
      "argv": ["make", "tests"],
      "intent": "Fix failing tests until make tests exits 0. Prefer production-code fixes when tests are correct."
    }
  }
}
